BUNCH-77: Step 3 persistence event store (envelope, optimistic concurrency, snapshot cache)

Adds the event store infrastructure to WildBunch.Persistence:
- StoredEventEntity: persistence envelope with StreamId, Sequence, EventId,
  OccurredAtUtc, EventType, PayloadJson (jsonb), CorrelationId, CausationId,
  SchemaVersion. Unique index on (StreamId, Sequence) for concurrency backstop.
- StoredEventEntityConfiguration: table shape, unique indexes, FK to GameSessions
- GameSessionEntity gains StreamVersion and SnapshotVersion columns
- WildBunchDbContext gains StoredEvents DbSet
- EF migration: EventStore (adds table + columns)

Event serialization:
- GameSessionJsonSerializer.Events.cs: SerializeEvent/DeserializeEvent for typed
  domain events. Event type name is the concrete type name. Unknown types throw.

Repository port extension (single port, no separate event-store interface):
- IGameSessionRepository.StoreAsync gains optional correlationId parameter
- IGameSessionRepository.GetEventStreamAsync reads typed events from the store
- EfGameSessionRepository.StoreAsync: stages snapshot upsert + event append +
  concurrency check on the same DbContext. NO SaveChangesAsync — UoW commits.
- EfGameSessionRepository.GetEventStreamAsync: reads and deserializes typed events
- ToAggregate: sets session version from entity StreamVersion via reflection

Optimistic concurrency:
- ConcurrencyException in Application.Games.Exceptions
- StoreAsync checks persisted StreamVersion vs expected version (session.Version
  minus uncommitted events count). Throws ConcurrencyException on mismatch.
- Unique DB index on (StreamId, Sequence) is the backstop for races.

Handler updates:
- All 16 existing handlers updated to use named cancellationToken parameter
- InMemoryGameSessionRepository test double updated for new interface

InternalsVisibleTo:
- Added WildBunch.Persistence and WildBunch.Integration.Tests so MarkEventsCommitted
  and GameSessionRehydrator.SetVersion are accessible

Tests prove:
- StoreAsync appends events to StoredEvents table
- Purchase appends StoreItemPurchased event
- ConcurrencyException on version mismatch (concurrent access proof)
- GetEventStreamAsync returns typed events in order
- GetEventStreamAsync fromVersion returns only events after that version
- Replay from events reconstructs same state as snapshot load (core ES proof)
- StoreAsync does not call SaveChangesAsync directly (UoW commits)

All 409 tests pass. EF migration validation passes.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
