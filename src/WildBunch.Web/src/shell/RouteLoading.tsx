// src/shell/RouteLoading.tsx
import styled from "styled-components";

export const RouteLoading = styled.div`
  display: grid;
  place-items: center;
  padding: 48px;
  color: var(--muted);
  font-size: 0.95rem;
`;
