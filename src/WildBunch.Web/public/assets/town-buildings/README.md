# Town Buildings Assets

`sprites/` is the shipped asset home.

`_pipeline/` is the intermediate home.

This subtree is intentionally split between human guidance in `docs/art/town-buildings/` and agent guidance in `.agents/art/town-buildings/`.
