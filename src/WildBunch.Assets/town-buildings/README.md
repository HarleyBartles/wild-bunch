# Town Buildings Assets

This subtree holds the town-building asset pipeline outputs and final sprite
assets.

- `_pipeline/` contains reviewable staging output
- `sprites/` contains final promoted assets

Do not use the web public folder as the working asset home.
