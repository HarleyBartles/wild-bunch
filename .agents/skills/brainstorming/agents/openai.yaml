version: 1
metadata:
  skill_name: brainstorming
  source_category: first_party
interface:
  display_name: Brainstorming
  short_description: Use when starting any creative work such as creating features, building components, adding functionality, or modifying behavior.
  default_prompt: Use /brainstorming to use when starting any creative work such as creating features, building components, adding functionality, or modifying behavior.
policy:
  products:
  - codex
  allow_implicit_invocation: true
