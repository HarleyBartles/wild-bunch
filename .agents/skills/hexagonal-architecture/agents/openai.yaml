version: 1
metadata:
  skill_name: hexagonal-architecture
  plugin: architecture-pack
  source_category: first_party
interface:
  display_name: Hexagonal Architecture
  short_description: Use when isolating domain logic from frameworks, UI, and databases through ports and adapters. Do not use when the domain is trivial or the project is a thin framework wrapper.
  default_prompt: Use when isolating domain logic from frameworks, UI, and databases through ports and adapters. Do not use when the domain is trivial or the project is a thin framework wrapper.
policy:
  allow_implicit_invocation: true
