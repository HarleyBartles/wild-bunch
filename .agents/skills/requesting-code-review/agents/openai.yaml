version: 1
metadata:
  skill_name: requesting-code-review
  source_category: first_party
interface:
  display_name: Requesting Code Review
  short_description: Obtain evidence-based review of completed work.
  default_prompt: Use requesting-code-review when completing tasks, implementing major features, or before merging to verify work meets requirements.
policy:
  products:
  - codex
  allow_implicit_invocation: true
