version: 1
metadata:
  skill_name: repo-worker-base
  source_category: first_party
interface:
  display_name: Repo Worker Base
  short_description: Portable hygiene and publication boundaries for repository work.
  default_prompt: Use repo-worker-base to apply portable worktree, source-custody, validation, and publication boundaries after using-superpowers-plus selects the workflow owner.
policy:
  allow_implicit_invocation: true
