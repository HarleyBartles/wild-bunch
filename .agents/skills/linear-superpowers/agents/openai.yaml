interface:
  display_name: Linear Superpowers
  short_description: Shape Linear work packets with the smallest applicable workflow skill.
  default_prompt: Use /linear-superpowers to shape Linear issues and issue tracks so they name the smallest applicable workflow skill, explain why it applies, list the evidence required to prove it was followed, and use /connector-safety before any Linear write or blocked-write recovery. If a Linear write is blocked, rejected, safety-filtered, permission-rejected, schema-rejected, or validation-rejected, switch into /connector-safety recovery instead of retrying from memory.
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
