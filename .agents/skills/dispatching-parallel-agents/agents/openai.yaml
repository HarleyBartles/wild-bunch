version: 1
metadata:
  skill_name: dispatching-parallel-agents
  source_category: first_party
interface:
  display_name: Dispatching Parallel Agents
  short_description: Dispatch independent tasks that do not share state or ordering constraints.
  default_prompt: Use dispatching-parallel-agents when facing 2+ independent tasks that can be worked on without shared state or sequential dependencies.
policy:
  products:
  - codex
  allow_implicit_invocation: true
