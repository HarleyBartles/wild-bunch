interface:
  display_name: Invariant Buster
  short_description: Check binding constraints before action.
  default_prompt: Use $invariant-buster before an action, answer, plan, dispatch, or durable mutation when binding constraints may be violated.
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
