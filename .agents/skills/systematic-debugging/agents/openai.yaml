version: 1
metadata:
  skill_name: systematic-debugging
  source_category: first_party
interface:
  display_name: Systematic Debugging
  short_description: Diagnose bugs, test failures, and unexpected behavior from evidence.
  default_prompt: Use systematic-debugging when encountering any bug, test failure, or unexpected behavior, before proposing fixes.
policy:
  products:
  - codex
  allow_implicit_invocation: true
