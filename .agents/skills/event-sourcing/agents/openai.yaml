version: 1
metadata:
  skill_name: event-sourcing
  source_category: first_party
interface:
  display_name: Event Sourcing
  short_description: Design event-sourced state, audit history, and temporal queries.
  default_prompt: Use event-sourcing when the system needs an audit log, temporal queries, or event-driven state reconstruction. Do not use when a simple relational model is enough or when strong immediate consistency is required.
policy:
  allow_implicit_invocation: true
