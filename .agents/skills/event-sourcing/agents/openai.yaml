version: 1
metadata:
  skill_name: event-sourcing
  plugin: architecture-pack
  source_category: first_party
interface:
  display_name: Event Sourcing
  short_description: Use when the system needs an audit log, temporal queries, or event-driven state reconstruction. Do not use when a simple relational model is enough or when strong immediate consistency is required.
  default_prompt: Use when the system needs an audit log, temporal queries, or event-driven state reconstruction. Do not use when a simple relational model is enough or when strong immediate consistency is required.
policy:
  allow_implicit_invocation: true
