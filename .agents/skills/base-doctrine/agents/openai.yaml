version: 1
metadata:
  skill_name: base-doctrine
  source_category: first_party
interface:
  display_name: Base Doctrine
  short_description: Cross-runtime doctrine contracts for prompt limits, tool honesty, evidence posture, doctrine routing, and output-shape authority.
  default_prompt: Use base-doctrine to resolve cross-project operating invariants not owned by a more specific skill, including prompt limits, evidence honesty, doctrine routing, and output-shape authority.
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
