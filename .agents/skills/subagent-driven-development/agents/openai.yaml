version: 1
metadata:
  skill_name: subagent-driven-development
  source_category: first_party
interface:
  display_name: Subagent Driven Development
  short_description: Use when executing implementation plans with independent tasks in the current session
  default_prompt: Use /subagent-driven-development to use when executing implementation plans with independent tasks in the current session
policy:
  products:
  - codex
  allow_implicit_invocation: true
