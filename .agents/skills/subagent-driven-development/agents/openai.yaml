version: 1
metadata:
  skill_name: subagent-driven-development
  source_category: first_party
interface:
  display_name: Subagent Driven Development
  short_description: Execute independent plan tasks through fresh subagents and review gates.
  default_prompt: Use subagent-driven-development when executing implementation plans with independent tasks in the current session.
policy:
  products:
  - codex
  allow_implicit_invocation: true
