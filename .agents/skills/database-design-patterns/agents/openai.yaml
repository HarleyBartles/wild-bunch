version: 1
metadata:
  skill_name: database-design-patterns
  source_category: first_party
interface:
  display_name: Database Design Patterns
  short_description: Guidance for designing relational database schemas, normalizing data, choosing keys and constraints, or applying transactions, indexing, and partitioning patterns.
  default_prompt: Use database-design-patterns when designing relational database schemas, normalizing data, choosing keys and constraints, or applying transactions, indexing, and partitioning patterns.
policy:
  allow_implicit_invocation: true
