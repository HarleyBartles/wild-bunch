interface:
  display_name: Unslop Superpowers
  short_description: Guard Superpowers work with repo-specific anti-slop controls.
  default_prompt: Use $unslop-superpowers to discover the active repo unslop profile, choose the smallest safe anti-slop action, turn profile findings into controls, non-goals, and evidence requirements, and use @connector-safety before any GPT connector write or blocked-write recovery.
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
