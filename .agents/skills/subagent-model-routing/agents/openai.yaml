version: 1
metadata:
  skill_name: subagent-model-routing
  source_category: first_party
  plugin: repo-worker-pack
  projection_plugin: repo-worker-pack
interface:
  display_name: Subagent Model Routing
  short_description: Use when choosing a child subagent model, reasoning level, or context mode.
  default_prompt: Use /subagent-model-routing before choosing a child subagent model, reasoning level, or context mode. Detect the live dispatch contract, load the shared policy and exactly one matching profile, then choose the least escalated exposed route.
policy:
  allow_implicit_invocation: true
