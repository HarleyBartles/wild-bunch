version: 1
metadata:
  skill_name: executing-plans
  source_category: first_party
interface:
  display_name: Executing Plans
  short_description: Use when you have a written implementation plan to execute in a separate session with review checkpoints
  default_prompt: Use /executing-plans to use when you have a written implementation plan to execute in a separate session with review checkpoints
policy:
  products:
  - codex
  allow_implicit_invocation: true
