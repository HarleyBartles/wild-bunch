version: 1
metadata:
  skill_name: writing-plans
  source_category: first_party
interface:
  display_name: Writing Plans
  short_description: Turn approved requirements into an executable implementation plan.
  default_prompt: Use writing-plans when you have a spec or requirements for a multi-step task, before touching code.
policy:
  products:
  - codex
  allow_implicit_invocation: true
