interface:
  display_name: Ambiguity Buster
  short_description: Resolve material ambiguity before acting.
  default_prompt: Use $ambiguity-buster before acting when unresolved ambiguity could cause the wrong scope, target, source route, authority, artifact, worker handoff, or answer.
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
