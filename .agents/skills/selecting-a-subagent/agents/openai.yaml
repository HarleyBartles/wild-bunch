version: 1
metadata:
  skill_name: selecting-a-subagent
  source_category: first_party
interface:
  display_name: Selecting a Subagent
  short_description: Choose a subagent profile, model, reasoning level, and context mode.
  default_prompt: Use selecting-a-subagent before dispatching a subagent. Detect the live dispatch contract, load the shared policy and exactly one matching profile, then choose the least escalated exposed route.
policy:
  allow_implicit_invocation: true
