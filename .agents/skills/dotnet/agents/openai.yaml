version: 1
metadata:
  skill_name: dotnet
  source_category: first_party
  plugin: dotnet-pack
  projection_plugin: dotnet-pack
interface:
  display_name: Dotnet
  short_description: Use when building or reviewing .NET ecosystem applications, C# language patterns, ASP.NET Core APIs, and common library choices.
  default_prompt: Use /dotnet when building or reviewing .NET ecosystem applications, C# language patterns, ASP.NET Core APIs, and common library choices.
policy:
  allow_implicit_invocation: true
