# Crew doctrine moved

Long-form Crew role doctrine now lives in the GPT-wide `crew` skill.

Use this file only as a compatibility pointer for older instructions that still reference `crew-buster/references/crew-lenses.md`.

When the role model, dependency chain, TPS handoff, proper-noun disambiguation, or golden-plan contract matters, read `skills://crew/SKILL.md` and its references instead.

`crew-buster` owns applying Crew reasoning to the current task; `crew` owns what the Crew are and how their role outputs compose.
