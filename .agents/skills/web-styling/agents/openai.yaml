version: 1
metadata:
  skill_name: web-styling
  plugin: frontend-pack
  source_category: first_party
interface:
  display_name: Web Styling
  short_description: Use when choosing or refactoring CSS approaches across CSS Modules, Sass, Less, and styled-components.
  default_prompt: Use when choosing or refactoring CSS approaches across CSS Modules, Sass, Less, and styled-components.
policy:
  allow_implicit_invocation: true
