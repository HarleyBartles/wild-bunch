version: 1
metadata:
  skill_name: web-styling
  source_category: first_party
interface:
  display_name: Web Styling
  short_description: Guidance for choosing or refactoring CSS approaches across CSS Modules, Sass, Less, and styled-components.
  default_prompt: Use web-styling when choosing or refactoring CSS approaches across CSS Modules, Sass, Less, and styled-components.
policy:
  allow_implicit_invocation: true
