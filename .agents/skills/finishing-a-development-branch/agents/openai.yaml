version: 1
metadata:
  skill_name: finishing-a-development-branch
  source_category: first_party
interface:
  display_name: Finishing A Development Branch
  short_description: Use when implementation is complete, all tests pass, and you need to decide how to integrate the work
  default_prompt: Use /finishing-a-development-branch to use when implementation is complete, all tests pass, and you need to decide how to integrate the work
policy:
  products:
  - codex
  allow_implicit_invocation: true
