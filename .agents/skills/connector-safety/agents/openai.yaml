version: 1
metadata:
  skill_name: connector-safety
  source_category: first_party
interface:
  display_name: Connector Safety
  short_description: Keep connector writes safe, narrow, auditable, and verified.
  default_prompt: Use connector-safety to keep connector and tool-side-effect work narrow, authorized, auditable, and verified, especially after a blocked or rejected call.
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
