fix: update seed codec doctrine and guard ClueSurfacingResolver negative modulo

AGENTS.md and WildBunch.GameContent/AGENTS.md doctrine updated to match
the expanded BUNCH-107 implementation: direct bit-packing codec (not
round-trip search), 40-entry town-name pool with 5-20 town range (not
6-8 with anchor towns), slot-based derivation via xorshift shuffle, and
prosperity/services palettes. Removes stale references to the deleted
StartingWorldDescriptorSeedMixer class.

ClueSurfacingResolver boring-mode index now uses safe-wrap
(((x % len) + len) % len) to handle negative townSlotIndex or
visitCount inputs, matching WantedPosterResolver's boring-mode guard.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
