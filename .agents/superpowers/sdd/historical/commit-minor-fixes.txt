fix: minor code review items (stale comments, file rename, xorshift seed=0 guard)

- Stale comments: GameSession.cs lines 105-106 and 167-168 said the
  unrelated-criminal turn-in flow was "once wired" / "as those flows
  land". The flow is now wired (SettleUnrelatedCriminalTurnIn /
  UnrelatedCriminalTurnInSettled). Updated comments to reflect the
  current state.
- File rename: GameSetupSeedCodeValidator.cs contained class
  StartingWorldDescriptorCodeValidator. Renamed the file to match the
  class name.
- xorshift seed=0: SeedWorldCatalog.DeriveTownNames used xorshift32
  for the Fisher-Yates shuffle. xorshift32 has 0 as a fixed point
  (produces all zeros), so a seed of 0 would make the shuffle a no-op.
  Added a guard: if seed == 0, set seed = 1. Added a boundary test
  verifying a non-trivial shuffle when all encoded fields are 0.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
