BUNCH-90: gate-aware true culprit force path, remove FalseLead, expanded layout, dropdown label

- ForceDevSaloonOverride now uses the same gate-aware IsEligibleSaloonPersonOfInterestCandidate
  as the candidate list. No special permanent true-culprit rejection. When the killer-release
  gate is locked, the true culprit is rejected with a gate-aware reason. When the gate is open,
  the true culprit is forceable like any other eligible suspect.
- Removed FalseLead from DevSaloonPoiKind enum, ForFalseLead factory, handler switch case,
  domain test, and ADR-0032. FalseLead was semantically identical to Citizen. The false-lead
  outcome belongs to the normal confrontation flow, not a separate force kind.
- Replaced ForceDevSaloonOverride_RejectsTrueCulprit with two gate-aware tests:
  RejectsTrueCulprit_WhenKillerReleaseGateIsLocked and AcceptsTrueCulprit_WhenKillerReleaseGateIsOpen.
  Same split at the application handler test level.
- Force dropdown label changed from "(first eligible)" to "Any eligible suspect (normal selection)".
- Expanded saloon panel layout now uses a two-column grid (left: context, active POI, hidden truth,
  pending override, force controls; right: suspects). Compact mode stays single-column.
  DevPanelRenderProps interface added to pass expanded state to panel render functions.
- Updated ADR-0032 to reflect FalseLead removal and gate-aware force path.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
